name: Github Actions

on: 
  pull_request:
  push:
    branches:
      - main
      - develop
      - code-tidyness

jobs:
  test:
    name: Running unit tests 🤘
    runs-on: ubuntu-latest

    steps:

    - name: Install Back 🔧
      run: npm install

    - name: Install Front 🔧
      run: cd client && npm install
    
    